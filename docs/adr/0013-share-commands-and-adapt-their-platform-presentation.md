# Share commands and adapt their platform presentation

Skill Deck uses one cross-platform Command Model with stable command IDs, availability rules, shortcut semantics and Context Menu actions, while adapting how that model is presented: macOS receives a complete system Menu Bar, and Windows/Linux use their native/application-menu conventions plus the shared Toolbar. This avoids both three divergent command systems and a macOS menu facade forced onto other desktops.
